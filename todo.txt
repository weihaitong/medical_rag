1、UI 增加界面
2、历史，每个用户独立的查询历史，为了防止token过于庞大，可以保留最近的K个历史
3、重写、重排、查询的正确性
4、日志：方便审查、排障。日志日志文件 + 按天/大小轮转RotatingFileHandler
5、模型：当前模型在本地个人电脑上运行，后需要更换为大模型提高效率
6、重写查询需要怎么做？
7、访问远程LLM服务
8、cuda、mps or cpu？
9、向量数据库每次都会重建？
10、如何判断rag的推理是否正确？